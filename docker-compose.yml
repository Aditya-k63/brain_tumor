version: "3.8"

services:
  app:
    build: .
    ports:
      - "8000:8000"
      - "8501:8501"
    env_file:
      - .env
    environment:
      - PORT=8000
      - RUN_STREAMLIT=true
      - API_URL=http://localhost:8000
    restart: unless-stopped
